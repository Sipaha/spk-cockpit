version: "2"
run:
  timeout: 3m
linters:
  default: standard
  enable:
    - errorlint
    - gocritic
    - gosec
    - misspell
    - revive
    - staticcheck
    - unparam
    - unused
formatters:
  enable:
    - gofmt
    - goimports
